# ─── Shared (kalshi/config.py) ──────────────────────────────────────────
# Kalshi API credentials. Used by every package that places trades.
# Get from https://kalshi.com/account/profile → API Keys → "Create new API key"
API_KEY_ID=
# Absolute path to the matching RSA private key .pem file
PRIVATE_KEY_PATH=

# ─── agent_trader ───────────────────────────────────────────────────────
# Anthropic API key. https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# ─── nba_trading (nba_trading/config.py) ────────────────────────────────
# How many contracts to bid per game.
SHARES_TO_BUY=10
# Price-segment bounds (cents). Only bid when ask ∈ [MIN, MAX].
FAVORITE_PRICE_MIN=0
FAVORITE_PRICE_MAX=0
# Local schedule cache file.
SCHEDULE_FILE=schedule_data.json
